<Window x:Class="DogApiApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DogApiApp"
        mc:Ignorable="d"
        Title="Dogs" Height="450" Width="800" MinHeight="450" MinWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Background="#33c4b3"/>
        <Grid Grid.Row="2" Background="#414b57"/>

        <TextBlock 
            Text="DogsViewer" 
            HorizontalAlignment="Center" 
            VerticalAlignment="Center"
            FontSize="30" 
            Foreground="#000" 
            FontFamily="Segoe Ui"/>

        <Frame Grid.Row="1" NavigationUIVisibility="Hidden" x:Name="MainFrame"/>
    </Grid>
</Window>
